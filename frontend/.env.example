# Base URL used by src/services/api.ts.
# Leave unset to use the Vite dev proxy (/api -> http://localhost:8000).
VITE_API_BASE_URL=http://localhost:8000

# Used by the legacy chat feature client (src/features/chat). Keep unset
# unless pointing that feature at a remote backend.
# VITE_API_URL=http://localhost:8000
